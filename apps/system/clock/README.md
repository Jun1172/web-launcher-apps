# ⏱️ 番茄钟（clock，系统应用）

一个极简的计时器，用来验证系统应用链路。

## 功能
- 单文件 HTTP 服务，监听 `127.0.0.1:8102`
- 显示 `HH:MM:SS` 计时
- 开始 / 暂停按钮

## 系统应用属性
- ✅ 默认安装（在 `apps/system/clock/`）
- ✅ 可通过应用商店升级
- ❌ 不可卸载（launcher 拒绝执行 `do_uninstall`）

## 文件
- `app.json` —— 应用清单
- `app.py` —— 单文件 HTTP 服务
