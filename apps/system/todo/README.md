# 📝 待办清单（todo，系统应用）

最简的待办列表 demo，验证系统应用链路。

## 功能
- 单文件 HTTP 服务，监听 `127.0.0.1:8101`
- 输入框 + 添加按钮
- 列表项点击即删除

## 系统应用属性
- ✅ 默认安装（在 `apps/system/todo/`）
- ✅ 可通过应用商店升级
- ❌ 不可卸载

## 文件
- `app.json` —— 应用清单
- `app.py` —— 单文件 HTTP 服务
