# { } JSON 工具 (json-tool)
验证 Launcher 承载纯前端工具类应用的能力。
## 应用行为
前端实现 JSON 字符串的语法校验、美化格式化与单行压缩。
## 验证步骤
1. 在 Launcher 中点击「JSON 工具」。
2. 输入一段未格式化的 JSON，点击「校验」，观察是否提示“格式完全正确”。
3. 故意输入错误语法（如缺少引号），点击校验，观察是否标红提示具体错误原因。