# { } JSON 工具 (json-tool)

![界面预览](images/screenshot.png)

纯前端 JSON 校验 / 格式化 / 压缩工具，无外部依赖。

- **精确报错**：解析失败时显示第几行第几列、出错行片段与 `^` 定位
- **常见错误提示**：自动识别尾逗号、单引号、未加引号的键名、括号未闭合、注释等
- 一键格式化（2 空格缩进）或压缩为单行，并复制结果
- 实时统计字符数 / 行数 / 字节数
